
2019年 03月 21日 星期四 14:05:27 CST

== Installation
 
  => Setup your sources.list
  	   sudo sh -c 'echo "deb http://packages.ros.org/ros/ubuntu $(lsb_release -sc) main" > /etc/apt/sources.list.d/ros-latest.list'
	   sudo apt-get update
  => Set up your keys
  	   sudo apt-key adv --keyserver hkp://ha.pool.sks-keyservers.net:80 --recv-key 421C365BD9FF1F717815A3895523BAEEB01FA116

  => Installation
	   sudo apt-get install ros-kinetic-desktop-full
	   		(Desktop-Full install)

	   echo "source /opt/ros/kinetic/setup.bash" >> ~/.bashrc
	   source ~/.bashrc
	   sudo apt-get install ros-kinetic-ros-control
	   在 /etc/ld.so.conf.d 目录中添加 ros.conf文件， ros.conf 文件中加入 '/opt/ros/kinetic/lib'
	   ;; sudo ldconfig

=> 环境设置
      echo "source /opt
      /ros/kinetic/setup.bash" >> ~/.bashrc
      source ~/.bashrc

=> catkin_ws 
      mkdir catkin_ws
      cd catkin_ws
      mkdir src
      catkin_make
      将源代码文件放到 src 下（如 src/soft_robot_research)
      catkin_make  ;; 在顶层执行（src上级目录）

== 初始化 rosdep
    我是在目录 /opt/ros/kinetic 里执行的。
    sudo rosdep init
    rosdep update

== ccache
    sudo apt-get install ccache

	man ccahe
	ccache -V
	ccache -s		; current statistics summary for the cache
	ccache -C -z	; Clear the entire cache, removing all cached files.
		   	  		  		(but keeping the configuration file) 
		   	  		; Zero the cache statistics.
					  	    (but not the configuration options)
== 对安装的简单测试
      执行 roscore
      在另一个 terminal 执行 turtlesim
	  
/opt/ros/kinetic/bin/roscore
/opt/ros/kinetic/lib/turtlesim/turtlesim_node
/opt/ros/kinetic/lib/turtlesim/turtle_teleop_key 
    
== uninstall
      => sudo apt-get remove ros-*
      => sudo apt-get autoremove
